# Environment Configuration Template for Courier Module
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ================================
# Django Settings
# ================================

# Environment: development, staging, production
DJANGO_ENV=development

# Django Secret Key - Generate using:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=your-secret-key-here

# Debug mode (MUST be False in production)
DEBUG=True

# ================================
# Security - REQUIRED
# ================================

# Admin password for X-Admin-Token authentication
# Requirements: 12+ characters, mix of letters, numbers, and symbols
ADMIN_PASSWORD=YourSecurePassword123!

# Allowed Hosts (comma-separated, REQUIRED for production)
# Example: ALLOWED_HOSTS=example.com,www.example.com,api.example.com
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS Allowed Origins (comma-separated, REQUIRED for production)
# Example: CORS_ALLOWED_ORIGINS=https://example.com,https://www.example.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ================================
# Database (Production - PostgreSQL)
# ================================
# For development, SQLite is used automatically
# For production, configure PostgreSQL:

DB_NAME=courier_db
DB_USER=courier_user
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# ================================
# Caching (Redis)
# ================================
# Optional: If not set, local memory cache is used
# REDIS_URL=redis://localhost:6379/1

# ================================
# Monitoring (Sentry)
# ================================
# Optional: Error tracking (production only)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ================================
# Production Checklist
# ================================
#
# Before deploying to production:
# 1. Set DJANGO_ENV=production
# 2. Set DEBUG=False
# 3. Generate a unique DJANGO_SECRET_KEY
# 4. Set strong ADMIN_PASSWORD (12+ chars, mixed complexity)
# 5. Configure ALLOWED_HOSTS (no wildcards!)
# 6. Configure CORS_ALLOWED_ORIGINS (no wildcards!)
# 7. Set up PostgreSQL and configure DB_* variables
# 8. (Optional) Configure Redis for caching
# 9. (Optional) Configure Sentry for error monitoring
#
# NEVER use default/example values in production!

